# Pix macOS design contract — dark overrides

Use system semantic colors and materials in dark mode. Keep the status dot
paired with its text, preserve high-contrast monospaced confirmation codes, and
avoid custom black surfaces that reduce native menu readability.
