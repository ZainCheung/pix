name: Relay Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    name: Validate and deploy Pix Relay
    uses: ./.github/workflows/relay-checks.yml
    with:
      deploy: true
